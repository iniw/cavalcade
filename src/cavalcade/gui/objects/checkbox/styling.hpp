#pragma once

namespace gui::personal {
	namespace padding {
		// the padding between a checkbox and it's label
		constexpr inline i32 label = 3;
	} // namespace padding

	namespace sizing {
		// the size of a checkbox
		constexpr inline render::size main{ 10, 10 };
	} // namespace sizing
} // namespace gui::personal