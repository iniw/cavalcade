#pragma once

namespace gui::personal {
	namespace padding {
		// the padding between a slider and it's label
		constexpr inline i32 label = 3;
	} // namespace padding

	namespace sizing {
		// the height of a slider
		constexpr inline i32 height{ 10 };
	} // namespace sizing
} // namespace gui::personal